abstract class SoundSetType {
  // createNew(name) => {};
  // saveAudio(type) => {};
  // duplicate(name) => {};
  // cache() => {};

  bool exists();

  // String name = '';
  // String description = '';
  // String repo = '';
  // String? download;
  // // path = Config.path / name.eragesoundset for installed soundsets
  // String? path;
  // Map? _plist;
  // bool tmp = false;

  // static void createNew(name) => {};
  // void saveAudio(type);
  // void duplicate(name);
  // Future cache();
  // void exists();
  // void delete();
  // void get();
  // void replace(type, file);
  // void replaceSelect(type) => {};

  // SoundSetType({
  //   required this.name,
  //   required this.description,
  //   required this.repo,
  //   this.download,
  //   this.path,
  //   this.tmp = false,
  // });

  // Map? get plist {
  //   if (path == null) return null;
  //   _plist ??= getPlist(path as String);
  //   return _plist as Map;
  // }

  // set plist(Map? plist) {
  //   _plist = plist;
  // }
}
